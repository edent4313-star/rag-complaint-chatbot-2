[flake8]
max-line-length = 100
exclude =
    __pycache__,
    .git,
    venv,
    .venv,
    tests/conftest.py
# Ignore line-too-long in test files (long mock patches are unavoidable)
per-file-ignores =
    tests/*.py: E501
